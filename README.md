# atanurgul.dev

Personal site. A single page of plain HTML, CSS and vanilla JavaScript.

No build step and no dependencies. Clone the repository and open `index.html`
in a browser, or serve the folder with any static file server.
